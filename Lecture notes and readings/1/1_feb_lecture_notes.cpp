/*
functions:
definiation in header file
implementation in seperate cpp file
call in main.cpp file


Overloading: making the same function with different inputs
int myFunction(int)
int& myFunction(bool)

can't be overloaded if arguments OR return types are the same



pass by value, uses the value of variable


pass by referance, uses the referance of the variable
*/